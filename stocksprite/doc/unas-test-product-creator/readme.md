Use the created `result.xml` with the `setProduct - add` postman collection to call the specific unas api to upload the products to unas.
Delete the existing products with the same sku first on Unas.
# Usage
`update.ps1`

