- IN PROGRESS, make queue 4 stages: cache -> pub -> convert(to unas xml) -> update(send to unas)
- what test data should be used for cromwell or magictools?
- integration tests?
- PUM-632940-37, varhato erkezes mennyiseg meg datum nem frissul
- when validating, the publish should go into a different queue that the validate script can use, 
    currently the subscribe uses the same queue so validating is not working on the server    
- refactor for multi user product, store data in redis (cache, settings, queues) by userid
- make the validate.js script part of the daily run to check for error continously (notify on error in redis)
- add some notification or checks that validate that cron jobs are run (e.h: `npm run cache:rel`), if not, send a notification
- !!!! for some reason -1 value appears for main stof for some products. investigate where does that come from. csv?
    ez azert jelenik meg mert ha valaki vasarol a 1 darabot termekbol az unas automatikusan levonja azt a foraktar keszletbol ami nalunk 0 mert a termknek nem jelenitjuk meg a foraktar keszletet. tehat ez nem hiba
    ezt ugy oldjuk meg az unason hogy: Unas: `További raktárak` -> `Főraktár beállítások` -> `Megrendelések megjelenjenek a főraktár mozgásánál`: bekapcsol, ami azt csinalja hogy: az adott beszallitoi raktarbol (pl depiend) ahol a termek szerpel, onnan vonja le azt az 1 darabot nem a foraktarbol. Ezt minden beszallitoi (unason `tovabbi raktar` be kell allitani)
- DONE, add redis connection and key prefix for dev mode configuration.development.json
- DONE, add wiremock fake unas api
- DONE, add wiremock mock server and mock unas server and test servers in code. code should not use testmode `if` conditions. remove clientMock class and dataConnectorMock for test data. add unas endpoint and scv provider mck endpoints
- DONE, remove images, description param handling and everything regarding image and description update
- DONE, check `// eslint` code and remove them
- DONE, move `worker` config section under `billMq` section
- DONE, Add ICOnfiguration functionality .NET style. appsettings. etc.
- DONE, fix callMappingFunction to use ruleids.
- DONE, fix datasource-json remove all "fn" and use ruleId for field manipulation
- IGNORED: data mapper testet megfixalni, a data mapper js kodot behozni az src/ ala